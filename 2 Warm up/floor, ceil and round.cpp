#include <bits/stdc++.h>
using namespace std;

int32_t main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	double d = 2.6;

	cout << floor(d) << '\n'; //low nearest
	cout << ceil(d) << '\n';  //high nearest
	cout << round(d) << '\n'; //nearest

	return 0;
}