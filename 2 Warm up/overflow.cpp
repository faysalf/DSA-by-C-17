#include <bits/stdc++.h>
using namespace std;

int32_t main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	int x = 1e9, y = 1e9;
	long long int an = x * y;
	long long int ans = (long long int) x * y;

	///first a int, int multiply korle integer a e rakhte chay.
	//long long thaka sotteo. tai multiple er age long long int declair kora lagbe

	cout << an << '\n' << ans << '\n';

 	return 0;
}