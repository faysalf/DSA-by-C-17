#include <bits/stdc++.h>
using namespace std;

int32_t main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	int n = 200;
	cout << sqrt(n) << '\n';	//square root
	cout << cbrt(n) << '\n';	//cubic root

	return 0;
}