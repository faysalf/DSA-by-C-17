#include <bits/stdc++.h>
using namespace std;

int32_t main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	double d = (double) 10000000000000 / 3;

	cout << round(d) << '\n';

	cout << llround(d) << '\n';  //for long long round

	return 0;
}